# Active Publish Target

Branch: `brian-second-final`

PlaceId: `89787959603872`

UniverseId: `10138560838`

Publish helper: `scripts/publish-brian-second-final.ps1`

Rules:

- Do not use the old first-account target on this branch.
- Do not store `ROBLOX_OPEN_CLOUD_API_KEY` in repo files or chat.
- Set `ROBLOX_OPEN_CLOUD_API_KEY` only in the active terminal session.
- Use `scripts/publish-brian-second-final.ps1` so the branch-specific place and universe identifiers are applied together.
